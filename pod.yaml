#kubectl run nginx --image nginx

apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: frontend
spec:
  containers: 
  - name: nginx
    image: nginx
    
 # kubectl apply -f pod.yaml
 # kubectl get pods
